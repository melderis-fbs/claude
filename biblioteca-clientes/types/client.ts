export interface Client {
  id: string;
  n: string;      // Nombre
  p: string;      // Profesión
  g: string;      // Negocio
  c: string;      // A quién ayuda
  cat: string;    // Nicho
  u: string;      // Instagram (URL)
  email: string;  // Email
  x: boolean;     // Caso de éxito
  t: string;      // Testimonio (URL)
}
